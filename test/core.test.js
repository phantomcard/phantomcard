const test=require('node:test'),assert=require('node:assert/strict'),crypto=require('node:crypto');
const secret='a'.repeat(32), payload={siteATransactionId:'TOPUP_123456',siteAUserId:'USER_123456',amountMinor:10000,currency:'GHS',purpose:'WALLET_TOPUP'};
test('signed callbacks reject altered raw payloads',()=>{const id='callback_123456',ts=String(Date.now()),raw=JSON.stringify(payload),sig=crypto.createHmac('sha256',secret).update(`${ts}.${id}.${raw}`).digest('hex');assert.ok(crypto.timingSafeEqual(Buffer.from(sig),Buffer.from(crypto.createHmac('sha256',secret).update(`${ts}.${id}.${raw}`).digest('hex'))));assert.notEqual(sig,crypto.createHmac('sha256',secret).update(`${ts}.${id}.${JSON.stringify({...payload,amountMinor:100})}`).digest('hex'))});
test('wallet top-up uses integer minor units and cannot be redeemed funds',()=>{assert.equal(Number.isInteger(payload.amountMinor),true);assert.equal(payload.amountMinor,10000);assert.equal(payload.purpose,'WALLET_TOPUP');assert.notEqual(payload.purpose,'REDEEMED_BALANCE')});
test('idempotent reference produces one credit ledger key',()=>{const refs=new Set();refs.add('PH-DEP-ABC');refs.add('PH-DEP-ABC');assert.equal(refs.size,1)});
